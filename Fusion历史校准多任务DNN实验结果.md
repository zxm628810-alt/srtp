# Fusion 历史校准多任务 DNN 实验结果

## 模型

模型包含两个输入分支：

1. 原始传感器分支：128 维 UCI 气体传感器特征；
2. 历史校准分支：每个通道相对最近 3 个历史 Batch 中位数的变化量。

两个分支融合后共享表示，并同时输出六类气体分类和 ppm 浓度回归。与组员的 Fusion DNN 不同，本模型**不输入当前测试 Batch 的编号或独热编码**；测试 Batch 的校准特征仅由之前批次计算，更贴近后续真实硬件部署。

## 权重选择

总损失为 `分类交叉熵 + λ × 标准化浓度 Smooth L1`。仅在 Batch 4～8 比较 λ=0.35、0.60：

| λ | 准确率 | Macro-F1 | MAE ppm | ≤50 ppm MAE |
|---:|---:|---:|---:|---:|
| 0.35 | 0.820 | 0.752 | **26.25** | **25.70** |
| 0.60 | **0.843** | **0.771** | 26.56 | 28.70 |

四项平均排名并列；根据项目“气体识别优先”的任务定位，以分类指标作为平局规则，固定选择 λ=0.60。Batch 9、10 未参与选择。

## 最终确认：Batch 9、10

| 模型 | 分类准确率 | Macro-F1 | 总体 MAE ppm | R² | ≤50 ppm MAE |
|---|---:|---:|---:|---:|---:|
| 历史窗口 3 单任务 DNN | **0.734** | **0.724** | 42.48 | 0.773 | 26.43 |
| Fusion 历史校准多任务 DNN | 0.721 | 0.715 | **34.98** | **0.839** | **10.90** |

分 Batch 结果：Batch 9 的 MAE 为 15.81 ppm、Batch 10 的 MAE 为 54.14 ppm；Batch 10 的低浓度 MAE 为 13.15 ppm。

## 结论

1. 融合“原始传感器 + 历史校准”并进行联合分类/浓度训练，对浓度预测有显著收益，尤其是低浓度预测；最终确认阶段低浓度 MAE 从 26.43 ppm 降至 10.90 ppm。
2. 该模型没有提高最终确认阶段的平均分类准确率，Batch 10 分类准确率为 67.6%，说明后期类别边界漂移仍是独立难题。
3. 因此，当前可以保留两个互补模型：历史窗口 3 单任务 DNN 用于优先保障分类稳定性；Fusion 历史校准多任务 DNN 用于优先保障浓度和低浓度估计。
4. 这比直接使用 Batch 编号的 Fusion DNN 更符合硬件部署条件，因为实际设备可以维护历史基线读数，但不应依赖“未来测试 Batch 编号”。

## 可复核文件

- 开发阶段：`fusion_history_dev_035/`、`fusion_history_dev_060/`
- 最终确认：`fusion_history_final_060/`
- 训练脚本：`run_fusion_history_multitask.py`
