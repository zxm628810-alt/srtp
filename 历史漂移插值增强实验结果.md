# 历史漂移插值增强 + MMD 实验结果

## 研究假设

传感器时序漂移会使未来 Batch 的样本偏离早期训练数据。若只使用当前已知的
历史 Batch，在同一种气体、相同浓度、相邻历史 Batch 的传感器样本之间做线性
插值，可能生成“中间漂移状态”，使 MMD 模型提前覆盖更多漂移路径。

## 严格防泄漏规则

测试 Batch 为 k 时，只使用 Batch 1 至 k-1 的真实样本生成合成数据；不使用
Batch k 或任何未来 Batch。插值仅发生在：

- 相同气体类别；
- 相同标准浓度；
- 相邻的已知历史 Batch。

合成特征为两个历史传感器读数的内部线性组合，合成数量分别设为真实训练样本的
0%、25% 和 50%。MMD 权重固定为此前选定的 `lambda=0.5`。

## 开发集结果（Batch 4--8）

| 插值比例 | 平均 Accuracy | 平均 Macro-F1 | 综合排名 |
|---|---:|---:|---:|
| **0%（普通 MMD）** | **85.94%** | **0.779** | **1** |
| 50% | 85.17% | 0.763 | 2 |
| 25% | 83.61% | 0.743 | 3 |

开发集选择的最佳比例为 **0%**。因此 Batch 9、10 最终确认保留普通 MMD，
没有将任何插值合成样本用于最终模型。

## 关键现象

25% 插值增强在多个 Batch 上下降明显。例如 Batch 7：

- Accuracy：79.4% 降至 74.7%；
- Macro-F1：0.776 降至 0.716；
- Toluene Recall：59.5% 降至 29.2%。

50% 插值的表现略优于 25%，但仍未超过不增强的普通 MMD。

## 结论

1. **简单历史线性插值不能带来稳定的漂移鲁棒性提升。**
2. 虽然插值没有使用未来数据，满足时序验证规范，但线性混合的传感器读数并不
   等价于真实传感器在中间时间状态下的物理响应。
3. 传感器漂移可能与材料老化、环境、气体浓度和非线性响应共同作用；合成样本
   因此可能偏离真实数据流形，并干扰气体类别边界。
4. 该负结果进一步支持当前主结论：普通 MMD 的温和分布对齐优于人为构造的
   漂移样本；若没有真实硬件长期采集数据，不应将线性插值增强作为部署模型的
   默认训练步骤。

## 可复核文件

- 脚本：`run_drift_interpolation_augmentation.py`
- 开发集选择：`drift_augmentation_results/augmentation_dev_selection.csv`
- 全部结果：`drift_augmentation_results/augmentation_results.csv`
- 运行日志：`drift_augmentation_run.log`
