# 滑动历史基线校准 DNN 实验结果

## 方法

新增模型 `dnn_sensor_history_baseline`。对每个 Batch b：

- 只选择 b 之前最近 3 个历史 Batch；
- 对每个 128 维传感器通道计算历史样本中位数，作为滑动基线；
- 构造“原始特征 + 相对滑动基线变化量”共 256 维输入；
- 预测 Batch b 时，基线只由 Batch 1 到 b-1 产生，未读取 Batch b 或未来批次的样本、标签或统计量。

这比此前固定使用最早 Batch 作为基线更符合设备长期运行时“利用近期历史状态校准”的实际情况。

## 滚动验证结果

| 测试 Batch | 准确率 | Macro-F1 |
|---:|---:|---:|
| 4 | 75.2% | 64.5% |
| 5 | 99.0% | 82.3% |
| 6 | 75.5% | 64.9% |
| 7 | 77.4% | 75.0% |
| 8 | 93.5% | 93.1% |
| 9 | 76.8% | 74.5% |
| 10 | 70.1% | 70.3% |

平均准确率为 **81.1%**，平均 Macro-F1 为 **74.9%**。

## 与已有模型的比较

- Batch 10：滑动历史基线 DNN 为 70.1%，高于普通 DNN（69.5%）和固定基线 DNN（68.1%），但低于 DNN + 批次号（71.2%）。
- Batch 8：滑动历史基线为 93.5%，接近固定基线 DNN 的 93.9%，两者均高于普通 DNN。
- Batch 7：滑动历史基线为 77.4%，优于普通 DNN（76.2%），但不及固定基线 DNN（81.7%）。

## 结论

滑动历史基线能够在部分后期批次改善模型表现，并使 Batch 10 相比普通 DNN 提升 0.5 个百分点；但其优势仍不稳定，尚不能作为最终抗漂移方案。该负结果同样具有研究价值：它说明仅依靠通道级历史中位数校准不足以完全恢复类别边界，下一步应测试困难类别加权训练与更强的自适应融合方法。
