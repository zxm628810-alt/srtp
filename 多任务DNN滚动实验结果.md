# 多任务 DNN：气体类别与浓度联合学习

## 模型设计

新增真正的双输出头 DNN（PyTorch 实现），输入为 128 维原始传感器特征与最近 3 个历史 Batch 校准特征的拼接。网络共享三层特征编码器，同时输出：

- 六类气体分类结果；
- 气体浓度 ppm 回归结果。

训练损失为“分类交叉熵 + 0.35 × 标准化浓度 Smooth L1 损失”。每个测试 Batch k 只用 Batch 1～k-1 计算校准特征、特征标准化、浓度标准化和训练验证划分。

## 与历史窗口 3 单任务 DNN 的比较

| 阶段 | 模型 | 分类准确率 | Macro-F1 | 浓度 MAE ppm | R² | ≤50 ppm MAE |
|---|---|---:|---:|---:|---:|---:|
| Batch 4～10 | 历史窗口 3 单任务 | **0.811** | **0.749** | 34.70 | 0.573 | 36.99 |
| Batch 4～10 | 多任务 DNN | 0.805 | 0.738 | **31.90** | **0.738** | **26.84** |
| Batch 9～10 | 历史窗口 3 单任务 | 0.734 | 0.724 | **42.48** | **0.773** | 26.43 |
| Batch 9～10 | 多任务 DNN | **0.758** | **0.742** | 48.28 | 0.689 | **22.81** |

Batch 10 中，多任务 DNN 的分类准确率为 74.5%、Macro-F1 为 73.4%，高于单任务模型的 70.1%、70.3%；低浓度浓度 MAE 为 15.17 ppm，优于单任务模型的 24.25 ppm。但其总体浓度 MAE 为 72.35 ppm，高于单任务模型的 62.65 ppm。

## 阶段结论

1. 联合训练可以改善部分未来 Batch 的分类识别和低浓度浓度预测，说明类别与浓度任务之间存在可共享的传感器响应信息。
2. 但在最终确认 Batch 9～10 中，多任务模型的总体浓度 MAE 高于历史窗口 3 单任务模型，说明当前损失权重下回归任务仍不够稳定。
3. 因此，多任务 DNN 保留为重要候选模型，而当前总体浓度部署主模型仍是历史窗口 3 单任务 DNN。
4. 下一轮可以只在历史训练 Batch 上选择分类/回归损失权重（例如 0.15、0.35、0.60），然后固定参数测试 Batch 9、10，以避免用未来 Batch 调参。

## 可复核文件

- 指标：`multitask_dnn_results/cross_batch_multitask_metrics.csv`
- 各 Batch 预测明细：`multitask_dnn_results/predictions_multitask_dnn_batch*.csv`
- 训练脚本：`run_multitask_dnn.py`
