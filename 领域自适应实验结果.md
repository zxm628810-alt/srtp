# 领域自适应（MMD）实验结果

## 目的

传感器漂移导致老 Batch 训练数据与新 Batch 测试数据之间存在分布偏移。本实验在训练 loss 中加入 MMD（Maximum Mean Discrepancy）正则化项，强制模型编码器将不同批次的传感器数据映射到相似的特征分布，从而对抗漂移带来的分布偏移，提升模型跨时序泛化能力。

## 方法

模型使用 PyTorch MLP（128→256→128→64→6），与 sklearn 版本架构一致。训练时，每个 mini-batch 内的样本按 batch_id 中位数分为"源域（老批次）"和"目标域（新批次）"，对编码器 64 维中间表示计算 MMD² 损失：

```
MMD² = E[k(z_s, z_s')] + E[k(z_t, z_t')] - 2·E[k(z_s, z_t)]
```

其中 k 为 RBF 高斯核，使用三个不同带宽（0.5×、1×、2× 中位数距离）取平均以提高鲁棒性。

总损失 = CrossEntropy（分类） + λ × MMD²（分布对齐）。

## 权重选择

仅在 Batch 4-8 上对比 λ=0（基线）、0.1、0.5、1.0，以平均准确率和 Macro-F1 等权排名选择最优 λ。Batch 9-10 未参与选择。

### 开发阶段结果（Batch 4-8 平均）

| λ | 平均准确率 | 平均 Macro-F1 | 准确率排名 | F1 排名 | 平均排名 |
|:---:|:---:|:---:|:---:|:---:|:---:|
| 0.0（基线） | 84.9% | 0.756 | 4 | 4 | 4.0 |
| 0.1 | 85.1% | 0.759 | 3 | 3 | 3.0 |
| **0.5** | **85.9%** | **0.779** | **1** | **1** | **1.0** |
| 1.0 | 85.6% | 0.774 | 2 | 2 | 2.0 |

λ=0.5 在 5 个 Batch 中 3 个最优（Batch 5/7/8），两项指标均排名第一，选定。

## 最终确认（Batch 9-10）

| 模型 | Batch 9 | Batch 10 | 平均 |
|------|:---:|:---:|:---:|
| 基线（无 MMD） | 77.4% | 67.6% | 72.5% |
| MMD λ=0.5 | 77.4% | **71.4%** | **74.4%** |

### Batch 10 逐气体 Recall

| 气体 | 基线 Recall | MMD λ=0.5 Recall | 变化 |
|------|:---:|:---:|:---:|
| Ethanol | 78.2% | 78.5% | +0.3% |
| **Ethylene** | **29.8%** | **36.2%** | **+6.4%** |
| Ammonia | 83.0% | 83.0% | 0 |
| Acetaldehyde | 85.8% | 85.2% | -0.6% |
| **Acetone** | **45.3%** | **58.7%** | **+13.4%** |
| Toluene | 83.3% | 86.7% | +3.4% |

## 结论

1. **MMD 领域自适应是当前所有实验中第一个能在 Batch 10 上实现稳定分类改善的方法（+3.8%）。**此前特征工程改进和选择性校准均未能在 Batch 10 取得正向收益。

2. **改善集中在最难区分的两类气体：乙 烯（+6.4%）和丙酮（+13.4%）。**这两类气体在此前的决策边界分析、PCA 漂移分析和错误分析中均被确认处于类别边界附近，容易因漂移被误判。MMD 通过强制编码器忽略批次差异，使这些边界附近的样本不再因分布偏移跨过决策边界。

3. Batch 9 准确率与基线一致（77.4%），表明 MMD 正则化没有损害未见批次的性能——它只改善困难批次，不影响相对容易的批次。

4. MMD 不增加模型参数、不改变网络结构、不依赖测试 Batch 信息，仅通过额外 loss 项约束编码器的中间表示分布，是一种轻量且可推广的领域自适应方案。

5. 对比此前所有改进实验（困难气体加权 -0.2%、IQR 校准 -2.2%、选择性校准 -1.2%），MMD 领域自适应是唯一在严格滚动验证下实现远期批次实质性分类增益的方法。

## 可复核文件

- 结果汇总：`domain_adaptation_results/domain_adaptation_results.csv`
- 对比图：`domain_adaptation_results/figures/领域自适应开发阶段λ选择.png`、`领域自适应最终确认.png`、`领域自适应Batch10逐气体Recall.png`
- 脚本：`run_domain_adaptation.py`
