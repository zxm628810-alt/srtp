# 最终浓度回归候选模型对比

## 比较规则

所有候选均使用同一 UCI 传感器时序数据及滚动规则：Batch 1～k-1 训练、Batch k 测试，测试 Batch 为 4～10。模型选择阶段为 Batch 4～8，Batch 9、10 作为后续确认阶段。MAE 单位为 ppm，越小越好；R² 越高越好。

| 候选模型 | 特征/方法 |
|---|---|
| 普通 DNN | 128 维原始传感器特征 |
| DNN + 批次号 | 原始特征加研究用时间索引 |
| 历史窗口 3 DNN | 原始特征加最近 3 个历史 Batch 中位数相对校准特征 |
| 低浓度加权 DNN | 历史窗口 3 特征；≤50 ppm 训练样本权重为 3 倍 |
| 历史残差校正 DNN | 历史窗口 3 DNN；用历史 Batch 的折外残差、按预测气体类别修正 |
| 两阶段 DNN | 先预测气体类别，再调用该类别专属浓度 DNN |

## 平均结果

| 阶段 | 模型 | 总体 MAE | 平均 R² | ≤50 ppm MAE |
|---|---|---:|---:|---:|
| Batch 4～8 | 低浓度加权 DNN | **27.92** | **0.672** | 40.45 |
| Batch 4～8 | 历史窗口 3 DNN | 31.59 | 0.492 | 41.22 |
| Batch 4～8 | 普通 DNN | 32.19 | 0.489 | **32.27** |
| Batch 9～10 | 历史窗口 3 DNN | **42.48** | 0.773 | 26.43 |
| Batch 9～10 | 历史残差校正 DNN | 45.04 | **0.791** | 21.06 |
| Batch 9～10 | 低浓度加权 DNN | 63.03 | 0.707 | **16.39** |
| Batch 4～10 | 历史窗口 3 DNN | **34.70** | 0.573 | 36.99 |
| Batch 4～10 | 低浓度加权 DNN | 37.95 | **0.682** | 33.57 |

完整六模型、三阶段对比见 `final_concentration_candidate_comparison.csv`。

## 选择结论

### 总体最佳模型：历史窗口 3 DNN

在 Batch 9、10 的确认阶段，其平均总体 MAE 为 42.48 ppm，是所有候选中最低；在全部 Batch 4～10 中平均 MAE 也最低（34.70 ppm）。因此它是当前的**主浓度预测模型**。

### 低浓度候选模型：低浓度加权 DNN

在 Batch 9、10 的 ≤50 ppm 样本上，其平均 MAE 为 16.39 ppm，最低，说明提高低浓度样本权重确实可能改善低浓度预测。但是 Batch 4 的低浓度 MAE 高达 134.31 ppm，且 Batch 10 总体 MAE 增至 105.27 ppm，稳定性不足。因此它目前只能作为**低浓度优化的研究候选**，不能直接取代主模型。

### 可部署时更合理的模型：历史窗口 3 DNN

真实硬件运行时没有“未来真实批次号”，也无法事先知道当前气体是否低于 50 ppm。因此：

1. 不把“批次号 DNN”作为实际部署主方案；它保留为证明时间辅助信息价值的研究对照。
2. 不把低浓度加权 DNN单独作为默认模型；因为它的适用场景在推理前未知，而且总体误差在部分未来 Batch 变大。
3. 优先选择历史窗口 3 DNN：它只需保存最近几批硬件基线读数，即可计算校准特征，不需要未来标签或未来批次信息。
4. 两阶段模型及残差校正暂不纳入主部署方案：前者会传递分类错误，后者在早期漂移批次不稳定。

## 项目关系

该比较说明：传感器时序漂移下不存在“所有批次、所有浓度都最优”的单一模型。项目的价值正是在统一滚动测试体系中，定量找出不同改进方法的收益、适用范围与失效条件；最终选择稳健且满足真实硬件信息约束的方法。
