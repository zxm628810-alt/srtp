# 增强历史统计校准 DNN 实验结果

## 方法

新增模型 `dnn_sensor_history_robust`。对每个 Batch b，仅使用 b 之前最近 3 个历史 Batch 的传感器样本，逐通道计算：

- 历史中位数；
- 历史四分位距 IQR（75% 分位数减 25% 分位数）；
- 当前读数相对历史中位数的偏移 `x - median`；
- 按历史波动范围标准化的偏移 `(x - median) / IQR`。

模型输入由 128 维原始传感器读数、128 维中位数偏移和 128 维 IQR 标准化偏移组成，共 384 维。预测 Batch b 时，当前 Batch 及未来 Batch 的样本、标签和统计量均未参与其校准统计计算。第一训练 Batch 没有更早历史时，才使用自身统计量作为有限值兜底。

## 滚动验证结果

| 测试 Batch | 准确率 | Macro-F1 |
|---:|---:|---:|
| 4 | 75.2% | 64.3% |
| 5 | 99.0% | 82.3% |
| 6 | 75.0% | 71.8% |
| 7 | 73.3% | 68.3% |
| 8 | 90.8% | 87.8% |
| 9 | 76.6% | 74.5% |
| 10 | 67.3% | 66.8% |

## 关键比较

- Batch 6：增强历史统计校准的 Macro-F1 为 71.8%，优于普通 DNN 的 63.8%，说明 IQR 特征在某些批次有助于类别均衡识别。
- Batch 10：准确率为 67.3%，低于普通 DNN（69.5%）、滑动历史基线 DNN（70.1%）和 DNN + 批次号（71.2%）。
- Batch 7、8 也未超过此前的固定基线或滑动历史基线模型。

## Batch 10 重点指标对照

所有模型均使用 Batch 1--9 训练、Batch 10 测试；低浓度指标统计真实浓度不高于 50 ppm 的 1,100 条测试样本。

| 模型 | 准确率 | Macro-F1 | 乙烯召回率 | 丙酮召回率 | 甲苯召回率 | ≤50 ppm 准确率 |
|---|---:|---:|---:|---:|---:|---:|
| 普通 DNN | 69.5% | 69.8% | 39.5% | 45.7% | 79.0% | 65.3% |
| DNN + 批次号 | **71.2%** | **71.7%** | **43.8%** | 42.0% | **83.7%** | 64.5% |
| DNN + 滑动历史中位数 | 70.1% | 70.3% | 39.5% | 50.5% | 80.0% | 64.4% |
| DNN + 历史中位数/IQR | 67.3% | 66.8% | 31.3% | **56.3%** | 74.0% | 56.5% |

增强 IQR 模型只在丙酮召回率上提高，其他核心指标均不占优。因此不能认为该模型总体优于三个对照模型；它更适合作为“统计校准对不同气体存在差异化作用”的证据。

## 结论

历史中位数与 IQR 特征可以描述传感器通道的相对偏移及其历史波动范围，但本次固定 3 批窗口的实现没有带来稳定的跨时序收益。可能原因是不同气体的响应模式和浓度变化并不能仅用逐通道统计校正恢复，384 维扩展输入也可能增加模型学习难度。

该实验说明：抗漂移不应只依赖单一的通道级归一化。下一步应从两条路线选择其一：

1. 在训练历史内部选择窗口大小与特征组合，避免固定使用 3 批窗口；
2. 将表现相对更好的“批次号/历史基线”信息与困难类别分析结合，针对乙烯、甲苯、丙酮设计更有针对性的融合或校准策略。
