# 决策边界覆盖测试结果

## 测试目的

本测试将申报书中的“决策边界覆盖测试”落实为可复核规则：不是只报告整体准确率，而是先从**未查看真实标签**的未来 Batch 样本中筛出可能处于类别边界的样本，再单独统计其错误率与典型混淆类型。

基础识别器为“DNN + 最近 3 个历史 Batch 校准特征”。对每个 Batch k，均以 Batch 1～k-1 训练，Batch k 只用于最终预测与评估。

## 固定的边界样本筛选规则

任一样本满足以下任一条件，即进入“边界覆盖样本集”：

1. **DNN 概率边界：**最大预测概率小于 0.70，或最高与第二高类别概率之差小于 0.25。
2. **PCA 几何边界：**在仅由历史训练数据拟合的标准化 PCA 二维空间中，计算到最近和第二近历史类别中心的距离 `d1`、`d2`；若 `(d2-d1)/(d1+d2) < 0.15`，则样本同时接近两类中心。

真实气体标签仅在样本筛选之后用于计算错误率；因此该过程没有使用测试标签进行边界选择。

## 结果

| 测试 Batch | 边界样本占比 | 边界样本错误率 | 非边界样本错误率 | 错误率差值 |
|---:|---:|---:|---:|---:|
| 4 | 36.6% | 33.9% | 19.6% | +14.3% |
| 5 | 41.1% | 0.0% | 1.7% | -1.7% |
| 6 | 49.4% | 47.1% | 2.4% | +44.7% |
| 7 | 49.8% | 42.6% | 2.9% | +39.7% |
| 8 | 31.0% | 11.0% | 4.4% | +6.6% |
| 9 | 59.8% | 35.6% | 4.8% | +30.8% |
| 10 | 55.2% | 30.0% | 29.9% | +0.0% |

## 关键分析

1. **Batch 6、7、9 验证了边界覆盖规则的有效性。**这些批次中，边界样本错误率分别比非边界样本高 44.7、39.7、30.8 个百分点，说明概率不确定或 PCA 中接近历史类别中心的样本，确实显著富集了易错样本。
2. **Batch 5 是简单批次。**几乎所有样本都正确，边界样本也没有形成高错误率。这表明本方法不会把每个 Batch 都强行解释为“边界失效”。
3. **Batch 10 是整体迁移失效，而不只是局部边界失效。**边界与非边界样本的错误率都约为 30%，说明传感器响应分布已整体偏离历史类别区域；仅覆盖边界样本不足以发现全部风险。这与此前 Batch 10 的总体准确率下降相互印证。
4. **关键混淆对：**Batch 9 的主要边界错误为甲苯→乙烯（91 个）；Batch 10 的主要错误为乙烯→甲苯（162 个）、丙酮→乙烯（112 个）、乙醛→氨气（69 个）。这为后续困难气体和传感器标定设计提供明确目标。

## 与申报书的对应关系

该实验满足申报书中“通过降维可视化筛选靠近气体类别决策边界、易出现识别混淆的边缘样本，并开展针对性测试”的要求，并进一步加入了可部署的 DNN 概率不确定性规则。它把 PCA 图从单纯展示，提升为可量化的边界样本测试流程。

## 可复核文件

- 指标：`decision_boundary_results/boundary_coverage_metrics.csv`
- 边界样本明细（含预测概率、PCA 距离间隔、真实/预测类别）：`decision_boundary_results/boundary_samples_batch*.csv`
- 边界集中的错误气体对：`decision_boundary_results/boundary_error_pairs.csv`
- PCA 图：`decision_boundary_results/figures/决策边界覆盖_PCA_Batch*.png`
- 运行脚本：`run_decision_boundary_coverage.py`
