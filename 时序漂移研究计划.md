# 时序漂移研究计划（八月模型工作）

## 研究问题

气体传感器经过不同批次/时间段后，其响应特征发生漂移；比较基础模型与融合时间信息的神经网络在未来批次上的识别稳定性。

## 数据

- `all_batches.csv`：13,910条样本，10个批次，6种气体，浓度与128维特征。
- `batch_id` 是严格时序测试的依据，不能随机打散后再作为最终结论。

## 对比模型

1. `rf_sensor`：128维传感器特征的 RandomForest 对照基线。
2. `dnn_sensor`：128维传感器特征的多层感知机（DNN）。
3. `dnn_sensor_time`：128维传感器特征与已知批次/设备使用时间特征融合的 DNN。

## 必做实验

### 固定早期训练、未来测试

训练 Batch 1–3，依次测试 Batch 4–10。输出每一批的 accuracy、macro-F1、各气体 recall、浓度 MAE、浓度 R2 和混淆矩阵。

### 滚动时序验证

训练 Batch 1..k-1，测试 Batch k（k=4..10）。这模拟系统随着时间获取历史数据后，对下一时段的预测能力。

## 已得到的校正后基线结果

RandomForest，训练 Batch 1–3：

| 测试批次 | Accuracy | Macro-F1 |
|---|---:|---:|
| 4 | 0.820 | 0.672 |
| 5 | 0.751 | 0.590 |
| 6 | 0.714 | 0.611 |
| 7 | 0.569 | 0.519 |
| 8 | 0.197 | 0.186 |
| 9 | 0.547 | 0.488 |
| 10 | 0.372 | 0.349 |

该结果显示随机划分的接近100%准确率不能代表未来时间段性能；Batch 8与10出现显著退化，支持开展漂移鲁棒性研究。

## 八月输出物

1. 可复用的数据转换程序：`prepare_uci_dataset.py`。
2. 统一数据集：`all_batches.csv`。
3. 跨批次基线指标与混淆矩阵：`drift_results_rf/`。
4. DNN 与融合 DNN 的同一套跨批次结果。
5. PCA可视化、性能随批次变化曲线、类别召回率热力图。
6. 中期报告中的实验方法、实验结果与结论。

## 严谨性要求

- 训练、标准化和特征选择不得使用测试批次数据。
- 批次特征只能表示测试时实际可获知的设备时间/批次，不能使用测试标签或未来统计量。
- 所有结果要报告各类别 recall，不只报告总准确率。
- `batch_id` 的原始类别映射已按官方每批样本数校正；氨气为原始类别5，丙酮为原始类别1。
