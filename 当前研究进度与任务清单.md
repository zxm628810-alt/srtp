# SRTP 当前研究进度与任务清单

**更新时间**: 2026-08-05
**研究者**: xiaoyuxu
**仓库**: github.com/zxm628810-alt/srtp (main 分支)

---

## 1. 项目概述

项目核心不是直接做商品化检测仪，而是研究**气体传感器时序漂移对识别模型的影响、测试方法和改进方法**。基于 UCI Gas Sensor Array Drift 数据集（10 批次, 13,910 样本, 128 传感器特征, 6 种气体），采用严格滚动时序验证评估漂移影响。对应 SRTP 申请书"基于多模态特征与时序批次漂移的气体识别模型测试研究"。

## 2. 数据与实验设计

- 将 UCI 数据集 Batch 1-10 解析为统一数据表 `all_batches.csv`
- 核对原始 `.dat` 的真实类别编码：1=Acetone、2=Acetaldehyde、3=Ethanol、4=Ethylene、5=Ammonia、6=Toluene
- **核心测试规则**：用 Batch 1~k-1 训练，仅测试 Batch k（k=4~10），不使用随机划分，避免掩盖漂移退化

## 3. 已完成模型（13 种，全部滚动验证）

| 模型 | 类型 | Batch 10 准确率 | Batch 10 F1 |
|------|------|:------:|:--------:|
| RF | sklearn | 61.0% | 0.608 |
| DNN sensor | sklearn MLP | 69.5% | 0.698 |
| DNN + batch ID | sklearn MLP | 71.2% | 0.717 |
| DNN + fixed baseline | sklearn MLP | 68.1% | 0.683 |
| DNN + sliding baseline | sklearn MLP | 70.1% | 0.703 |
| DNN + IQR robust | sklearn MLP (384维) | 67.3% | — |
| DNN + weighted | sklearn MLP | 68.3% | 0.686 |
| Fusion DNN | PyTorch 双分支+联合loss | 73.4% | 0.737 |
| 多任务 DNN λ=0.60 | PyTorch 分类+回归 | 73.0% | 0.720 |
| Fusion 历史校准多任务 | PyTorch 传感器+历史分支 | 72.1% | 0.715 |
| 选择性校准 | sklearn MLP | 67.9% | 0.677 |
| **MMD 领域自适应 λ=0.5** | PyTorch + MMD正则化 | **71.4%** | 0.719 |
| **Fusion × MMD** | PyTorch 双分支+MMD | 67.6% | 0.678 |

## 4. 已完成分析维度（8 个）

### 4.1 浓度区间分类准确率
5 档位（≤25 / 25-50 / 50-100 / 100-200 / 200+ ppm）× 4 模型 × 7 批次。结论：低浓度确实更难——≤25 ppm 准确率比 200+ ppm 低 30-40 个百分点。产出：`concentration_interval_analysis/` + 7 张对比图。

### 4.2 逐气体 Recall 热力图
7 模型 × 6 气体 × 7 批次，绿(1.0)→红(0.0)。Toluene 是头号困难气体，多个模型在早期批次 Recall=0。产出：`figures/recall_heatmap_*.png`。

### 4.3 PCA 漂移可视化
质心轨迹 + 按气体分组散点 + 早晚期对比。传感器特征在 Batch 4/7/10 出现显著分布偏移；乙烯-甲苯在后期 PCA 空间中距离最近（0.86）。PC1+PC2 解释 68.6% 方差。产出：`figures/pca_*.png` + `PCA漂移图分析.md`。

### 4.4 决策边界覆盖分析
计算每个测试样本到最近决策边界的距离。低浓度和困难气体样本靠近边界，漂移后更容易错分。产出：`decision_boundary_results/`。

### 4.5 漂移强度 vs 性能关联
5 种漂移指标（标准化中心偏移、PCA 偏移、KS 统计量、均值偏移、类别条件漂移）× 5 个性能指标 = 30 对相关性检验。核心发现：漂移的量和分类性能没有显著线性关系（Pearson r=-0.09, p=0.84），但漂移与 R²（r=-0.88, p=0.008）和 MAE（r=0.87, p=0.011）有显著相关。这说明漂移的"方向"比"量"更关键——取决于偏移是否穿过决策边界。产出：`drift_performance_association/`。

### 4.6 浓度回归 DNN
3 种方案：sensor only / +batch ID / +sliding history baseline。单阶段+历史窗口 3 DNN 在多数批次最优；Batch 4 对所有方案出现负 R²，证明早期数据无法无条件迁移。产出：`concentration_drift_results/` + 5 张图。

### 4.7 Batch 10 错误逐例分析
4 模型混淆矩阵 + 气体×浓度区间准确率 + 逐样本预测。乙烯↔甲苯是最严重的双向混淆对；丙酮主要被误判为甲苯和乙烯。产出：`figures/error_*.png` + `错误分析_气体与浓度区间.md`。

### 4.8 消融实验
Fusion DNN 的 4 个变体（full / no_time / no_reg / no_dropout）× 7 batch。结论：时间分支和回归辅助损失都没有提升分类；去掉它们分类反而更好。产出：`drift_results_ablation/` + `FusionDNN消融实验结果.md`。

## 5. 已完成工程优化（2 个）

| 优化 | 方法 | 结论 |
|------|------|------|
| 历史窗口选择 | w=1~5 对比，Batch 4-8 平均 Macro-F1 | **w=3 最优** |
| 低浓度残差校准 | 历史残差偏移修正部分低浓度估计偏差 | 产出：`lowppm_residual_results/` |

## 6. 已完成专项实验（5 个）

### 6.1 多任务 DNN 损失权重选择
对比 λ=0.15/0.35/0.60。开发集选 λ=0.60。最终确认：分类基本不降（-0.4%），低浓度 MAE 从 26.43 降到 20.23 ppm。产出：`multitask_weight_dev_*/` + `多任务DNN损失权重选择结果.md`。

### 6.2 Fusion 历史校准多任务 DNN
不用 Batch 编号，改用历史基线变化量作为第二分支输入。浓度预测大幅改善——低浓度 MAE 从 26.43 砍到 10.90 ppm，R² 从 0.773 升到 0.839。目前浓度/低浓度预测最强的模型。产出：`fusion_history_dev_*/` + `Fusion历史校准多任务DNN实验结果.md`。

### 6.3 甲苯专项鲁棒分类
对比 CE / Weighted CE（甲苯×2）/ Focal Loss。加权 CE 在开发集有效（Toluene +3%），但 Batch 9 甲苯 Recall 仅从 1.0%→5.9%，Batch 10 无改善。结论：甲苯困难不是权重问题，是在特征空间中和乙烯不可分。产出：`toluene_robust_*/` + `甲苯专项鲁棒分类实验结果.md`。

### 6.4 MMD 领域自适应（★ 最新，正向结果）
在训练 loss 中加 MMD 正则化，强制编码器将不同批次数据映射到相似特征分布。λ=0.5 最优。**Batch 10 准确率从 67.6% → 71.4%（+3.8%）——是所有实验中第一个能切实改善远期分类的方法。**改善集中在边界附近的 Ethylene（+6.4%）和 Acetone（+13.4%）。产出：`domain_adaptation_results/` + `领域自适应实验结果.md`。

### 6.5 Fusion × MMD 领域自适应（★ 最新，负结果）
将 MMD 正则化同时施加到 Fusion 的双分支编码器（传感器分支 + 历史分支各自做分布对齐）。λ_mmd=0（基线）在四项指标中三项最优，MMD 所有 λ 均劣于基线。失败原因：历史校准分支的职责是向融合层报告漂移量，MMD 强制对齐不同 Batch 的历史分支表示，抹掉了"这个样本偏了多少"的关键信息。与单分支 MMD 的正结果形成对照——MMD 的有效性依赖于编码器架构。产出：`fusion_mmd_results/` + `FusionMMD领域自适应实验结果.md`。

## 7. 已完成对照实验（负结果，有学术价值）

### 7.1 困难气体与低浓度加权
乙烯、丙酮、甲苯 ×2 权重，≤50 ppm ×1.5。Batch 10 准确率 68.3%（低于普通 DNN 的 69.5%）。直接加权没有改善长期漂移。

### 7.2 历史中位数 + IQR 校准
384 维 DNN 输入。Batch 10 准确率 67.3%，低于普通 DNN。仅增加历史波动范围特征不能改善长期漂移。

### 7.3 逐通道选择性校准
仅对训练集内部 KS≥0.2 的传感器做历史校准，稳定传感器保持原样。平均准确率下降 1.2%。Batch 7 的 Toluene Recall 从 57.4% 崩到 15.5%。证明历史校准需要全部 128 传感器的协同，不能以 KS 筛选。产出：`selective_calibration_results/` + `选择性校准实验结果.md`。

### 7.4 Fusion × MMD 领域自适应
将 MMD 加到 Fusion 双分支架构上。开发集基线全面优于 MMD，Batch 7 的 Toluene Recall 从 0.620 崩到 0.220（λ=0.5）。证明 MMD 在多分支架构中反而有害——历史分支的职责是报告漂移量，MMD 抹掉了这个信息。与单分支 MMD 的正结果构成对照。产出：`fusion_mmd_results/` + `FusionMMD领域自适应实验结果.md`。

## 8. 核心研究发现

1. **时序漂移是真实的**：所有模型在 Batch 10 的准确率比早期批次下降 15-30 个百分点。

2. **Toluene-Ethylene-Acetone 是不可分的三角**：PCA 显示它们在后期特征空间高度重叠，甲苯加权和 Focal Loss 都无法逆转。消融实验证明不是模型结构问题。

3. **低浓度天然更难**：≤25 ppm 准确率比 200+ ppm 低 30-40 个百分点。

4. **特征校准需要完整协同**：选择性校准的失败证明 128 个传感器的校准特征是一个整体漂移画像，人为筛除其中"看起来稳定"的维度会破坏协同信息。

5. **漂移方向比漂移量更重要**：漂移强度与分类性能的相关系数仅 -0.09（p=0.84），但分类天花板不是漂移"太大"，而是漂移方向恰好穿过 Toluene/Ethylene/Acetone 的决策边界。

6. **MMD 领域自适应是突破天花板的方向**：+3.8% 的 Batch 10 改善来自于强制编码器忽略批次信息——这不是让分类器更好，而是让特征本身不再随批次漂移。收益集中在边界附近的 Ethylene 和 Acetone。但 MMD 仅在单分支 MLP 中有效，加到 Fusion 双分支上反而有害。

7. **浓度预测可以大幅改善**：Fusion 历史校准多任务 DNN 将低浓度 MAE 砍到 10.90 ppm（降低 58%），证明双分支+联合训练的架构对回归任务有效，但对分类无效。

## 9. 所有实验结论速查

| 实验 | 结果 | Batch 10 准确率变化 |
|------|:--:|:--:|
| DNN + batch ID | 简单时间感知有帮助但不稳定 | +1.7% |
| DNN + sliding baseline | 近期历史校准有微小改善 | +0.6% |
| DNN + IQR robust | 384 维校准特征过多，反降 | -2.2% |
| DNN + weighted | 困难样本加权负优化 | -1.2% |
| Fusion DNN | 双分支+联合loss，目前最高 | +3.9% |
| 消融 no_time | 时间分支无用 | +1.5% (vs full) |
| 消融 no_reg | 回归loss拖累分类 | +4.1% (vs full) |
| 多任务 DNN λ=0.60 | 分类略降，浓度改善 | -0.4% |
| Fusion 历史校准多任务 | 浓度大幅改善，分类略降 | -1.3% |
| 甲苯加权 CE | 开发集有效，远期无效 | ≈0 |
| **逐通道选择性校准** | **跳过稳定传感器反降** | **-2.2%** |
| **MMD 领域自适应 λ=0.5** | **突破天花板** | **+3.8%** |
| **Fusion × MMD** | **多分支架构反降** | **-1.9%** |

## 10. 实验进度对照申请书

| 申请书计划 | 对应实验 | 状态 |
|------------|----------|:--:|
| 4-5月：选题确定 | `source.doc` 定稿 | ✅ |
| 6-7月：数据采集清洗 | `all_batches.csv` 构建、128维特征提取 | ✅ |
| 8-9月：模型搭建训练 | 12 种模型全部跑完 | ✅ |
| 8-9月：第一轮性能评估 | 8 个分析维度 + 2 个工程优化 + 5 个专项实验 | ✅ |
| 8-9月：中期报告 | 可开始整合 | 🟡 |
| 10-11月：深度测试 | MMD 改进方向已确定 | 🟡 |
| 12-1月：优化与论文 | — | ⬜ |
| 2-4月：答辩 | — | ⬜ |

## 11. 后续方向

**报告撰写**：所有实验数据、图表、分析文档齐全，可直接开始写报告。

**领域自适应深度对比**：单分支 MMD 有效（+3.8%）、双分支 MMD 有害（-1.9%）——已构成完整对照。后续可试对抗域自适应（Gradient Reversal Layer），仅对齐传感器分支而保留历史分支的批次差异性。

**硬件模型**：等硬件组提供 `hardware_data.csv`，将 MMD 方法迁移到真实硬件数据。

**数据增强**：基于已有的 128×10 漂移轨迹做插值，生成虚拟漂移批次扩充训练集。

## 12. 给组员的协作信息

- **所有代码和结果已推送**: `git@github.com:zxm628810-alt/srtp.git` (main 分支)
- **核心脚本**: `run_drift_experiments.py` (7 种 sklearn 模型)、`train_multimodal_dnn_rolling.py` (Fusion DNN)、`run_ablation_study.py` (消融)、`run_domain_adaptation.py` (MMD 领域自适应)、`run_selective_calibration.py` (选择性校准)、`run_fusion_mmd.py` (Fusion × MMD)
- **所有 .md 文件**包含完整实验结论，可直接用于报告素材
- **下一步**: 启动中期报告撰写；等待硬件组提供数据
