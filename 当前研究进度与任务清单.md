# SRTP 当前研究进度与任务清单

**更新时间**: 2026-08-04  
**研究者**: xiaoyuxu  
**仓库**: github.com/zxm628810-alt/srtp (main 分支)

---

## 1. 项目概述

项目核心不是直接做商品化检测仪，而是研究**气体传感器时序漂移对识别模型的影响、测试方法和改进方法**。基于 UCI Gas Sensor Array Drift 数据集（10 批次, 13,910 样本, 128 传感器特征, 6 种气体），采用严格滚动时序验证评估漂移影响。

## 2. 数据与实验设计

- 将 UCI 数据集 Batch 1-10 解析为统一数据表 `all_batches.csv`
- 核对原始 `.dat` 的真实类别编码：1=Acetone、2=Acetaldehyde、3=Ethanol、4=Ethylene、5=Ammonia、6=Toluene
- **核心测试规则**：用 Batch 1~k-1 训练，仅测试 Batch k（k=4~10），不使用随机划分，避免掩盖漂移退化

## 3. 已完成模型（7 种，全部滚动验证）

| 模型 | 类型 | 特征 | Batch 10 准确率 | Batch 10 Macro-F1 |
|------|------|------|:------:|:--------:|
| RF | sklearn | 128 传感器原始值 | 61.0% | 0.608 |
| DNN sensor | sklearn MLP | 128 传感器原始值 | 69.5% | 0.698 |
| DNN + batch ID | sklearn MLP | + 批次号 | 71.2% | 0.717 |
| DNN + fixed baseline | sklearn MLP | + 相对最早 batch 基线的 128 维变化率 | 68.1% | 0.683 |
| DNN + sliding baseline | sklearn MLP | + 滑动前 3 个 batch 中位数的 128 维相对变化 | 70.1% | 0.703 |
| DNN + IQR robust | sklearn MLP | + 128 维中位数偏移 + 128 维 IQR 标准化 (384 维) | 67.3% | — |
| DNN + weighted | sklearn MLP | 困难气体×2 + 低浓度×1.5 样本加权 | 68.3% | 0.686 |
| **Fusion DNN** | **PyTorch 双分支** | **传感器分支 + 时间分支 + 联合 loss** | **73.4%** | **0.737** |

> Fusion DNN 在 Batch 10 取得最高准确率 73.4%，但 Toluene 在 Batch 4/5/6/9 的召回率极低（0.00），仍需改进。

## 4. 已完成分析维度（6 个）

### 4.1 浓度区间分类准确率
- 5 档位（≤25 / 25-50 / 50-100 / 100-200 / 200+ ppm）× 4 模型 × 7 批次
- 结论：≤25 ppm 准确率普遍低于 50%，低浓度确实更难分类；Batch 6 中 ≤25 ppm 仅 31.9%（DNN sensor），而 200+ ppm 高达 95.4%

### 4.2 逐气体 Recall 热力图
- 7 模型 × 6 气体 × 7 批次，绿(1.0)→红(0.0)
- Toluene 是最困难气体，多个模型在早期批次 Recall=0；Ethylene 在 Batch 7/10 表现差

### 4.3 PCA 漂移可视化
- 质心轨迹图 + 按气体分组散点图 + 早晚期对比
- 传感器特征在 Batch 4/7/10 出现显著分布偏移

### 4.4 决策边界覆盖分析
- 计算每个测试样本到最近决策边界的距离
- 低浓度和困难气体样本更靠近边界，漂移后更容易错分

### 4.5 漂移强度 vs 性能关联
- 逐 batch 分布漂移量（均值偏移+方差变化）与模型精度损耗呈负相关
- 分布偏移大的批次对应低准确率

### 4.6 浓度回归 DNN
- 3 种方案：sensor only / +batch ID / +sliding history baseline
- 单阶段 + 历史窗口 3 DNN 在多数批次最优；Batch 4 对所有方案出现负 R²，证明早期数据无法无条件迁移

## 5. 已完成工程优化（3 项）

| 优化 | 方法 | 结论 |
|------|------|------|
| 历史窗口选择 | w=1~5 对比，以 Batch 4-8 平均 Macro-F1 为主指标 | **w=3 最优** |
| 低浓度残差校准 | 历史残差偏移修正 | 可部分改善低浓度估计 |
| 两阶段浓度模型 | 先分类再专属回归 | 未稳定优于单阶段；分类错误会传递 |

## 6. 核心研究发现

1. **时序漂移是真实的**：所有模型在 Batch 10 的准确率比早期批次下降 15-30 个百分点。

2. **Toluene 是致命弱点**：几乎所有模型的 Toluene 召回率在早期批次(Batch 4-6)为 0。Fusion DNN 在 Batch 9 仍跌落至 0.01。

3. **低浓度更难**：≤25 ppm 样本的分类准确率平均比 200+ ppm 低 30-40 个百分点。

4. **特征校准有帮助但不稳定**：滑动历史基线在某些批次改善显著，但在 Batch 10 增益有限。

5. **Fusion DNN 是最优架构但需消融验证**：双分支+联合 loss 在 Batch 10 准确率最高(73.4%)，需消融实验确认各组件的贡献。

6. **漂移与性能关联明确**：分布偏移大的批次(Batch 4/7/10)对应低准确率，决策边界附近样本更容易因漂移被错分。

## 7. 模型改进实验总结

### 7.1 困难气体与低浓度加权
对乙烯、丙酮、甲苯训练样本设置 2 倍权重，≤50 ppm 样本 1.5 倍权重。Batch 10 准确率从 69.5% 降至 68.3%，低浓度准确率从 65.3% 降至 63.6%。直接困难样本加权不足以稳定解决漂移问题。

### 7.2 历史中位数 + IQR 校准
形成 384 维 DNN 输入。Batch 6 的 Macro-F1 达到 71.8%，但 Batch 10 准确率为 67.3%，低于普通 DNN。仅增加历史波动范围特征不能稳定改善长期漂移。

### 7.3 滑动历史基线窗口选择
窗口 3 最优，已作为默认设置。改善了校准策略的规范性，但 Batch 9 的甲苯和 Batch 10 的乙烯/丙酮仍是主要难点。

## 8. 待完成

| 任务 | 状态 |
|------|:--:|
| **Fusion DNN 消融实验** (拆时间分支 / 拆回归 loss / 关 Dropout) | 🔴 脚本已写好（`run_ablation_study.py`），未运行 |
| 硬件数据采集与硬件模型训练 | 🔴 等待硬件组提供 `hardware_data.csv` |
| 中期报告撰写 | 🟡 可开始整合现有结果 |

## 9. 给组员的协作信息

- **所有代码和结果已推送**: `git@github.com:zxm628810-alt/srtp.git` (main 分支)
- **核心脚本**: `run_drift_experiments.py` (7 种模型)、`train_multimodal_dnn_rolling.py` (Fusion DNN)
- **分析脚本**: `plot_recall_heatmap.py`、`analyze_concentration_accuracy.py`、`plot_pca_drift.py` 等
- **下一步**: 需要硬件组提供 `hardware_data.csv` 后可启动硬件模型训练
