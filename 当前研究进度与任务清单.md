# SRTP 当前研究进度与任务清单

**更新时间**: 2026-08-06
**研究者**: xiaoyuxu
**仓库**: github.com/zxm628810-alt/srtp (main 分支)

---

## 1. 项目概述

项目核心是研究**气体传感器时序漂移对识别模型的影响、测试方法和改进方法**。基于 UCI Gas Sensor Array Drift 数据集（10 批次, 13,910 样本, 128 传感器特征, 6 种气体），采用严格滚动时序验证评估漂移影响。对应 SRTP 申请书"基于多模态特征与时序批次漂移的气体识别模型测试研究"。

## 2. 数据与实验设计

- 将 UCI 数据集 Batch 1-10 解析为统一数据表 `all_batches.csv`
- 核对原始 `.dat` 的真实类别编码：1=Acetone、2=Acetaldehyde、3=Ethanol、4=Ethylene、5=Ammonia、6=Toluene
- **核心测试规则**：用 Batch 1~k-1 训练，仅测试 Batch k（k=4~10），不使用随机划分，避免掩盖漂移退化
- 所有改进方案需经**开发集（Batch 4-8）选择 + 最终确认（Batch 9-10）**两步验证，关键正向结果需经**多随机种子稳定性验证**确认可复现性

## 3. 已完成模型（13 种，全部滚动验证）

| 模型 | 类型 | Batch 10 准确率 | Batch 10 F1 | 说明 |
|------|------|:------:|:--------:|------|
| RF | sklearn | 61.0% | 0.608 | 传统基线 |
| DNN sensor | sklearn MLP | 69.5% | 0.698 | 改进基准 |
| DNN + batch ID | sklearn MLP | 71.2% | 0.717 | 简单时间感知 |
| DNN + fixed baseline | sklearn MLP | 68.1% | 0.683 | 固定出厂基线 |
| DNN + sliding baseline | sklearn MLP | 70.1% | 0.703 | 滑动历史基线 |
| DNN + IQR robust | sklearn MLP (384维) | 67.3% | — | 过犹不及 |
| DNN + weighted | sklearn MLP | 68.3% | 0.686 | 样本加权负优化 |
| Fusion DNN | PyTorch 双分支+联合loss | 73.4% | 0.737 | 需Batch ID，不可部署 |
| 多任务 DNN λ=0.60 | PyTorch 分类+回归 | 73.0% | 0.720 | 浓度改善 |
| Fusion 历史校准多任务 | PyTorch 传感器+历史 | 72.1% | 0.715 | 浓度最优 |
| 选择性校准 | sklearn MLP | 67.9% | 0.677 | 负结果 |
| **MMD 领域自适应 λ=0.5** | **PyTorch + MMD** | **71.4%** | **0.719** | **★ 分类最优（可部署）** |
| Fusion × MMD | PyTorch 双分支+MMD | 67.6% | 0.678 | 负结果 |

## 4. 已完成分析维度（8 个）

### 4.1 浓度区间分类准确率
5 档位（≤25 / 25-50 / 50-100 / 100-200 / 200+ ppm）× 4 模型 × 7 批次。结论：低浓度确实更难——≤25 ppm 准确率比 200+ ppm 低 30-40 个百分点。产出：`concentration_interval_analysis/` + 7 张对比图。

### 4.2 逐气体 Recall 热力图
7 模型 × 6 气体 × 7 批次，绿(1.0)→红(0.0)。Toluene 是头号困难气体，多个模型在早期批次 Recall=0。产出：`figures/recall_heatmap_*.png`。

### 4.3 PCA 漂移可视化
质心轨迹 + 按气体分组散点 + 早晚期对比。PC1+PC2 解释 68.6% 方差。乙烯-甲苯在后期 PCA 空间中距离最近（0.86）。产出：`figures/pca_*.png` + `PCA漂移图分析.md`。

### 4.4 决策边界覆盖分析
计算每个测试样本到最近决策边界的距离。低浓度和困难气体样本靠近边界，漂移后更容易错分。产出：`decision_boundary_results/`。

### 4.5 漂移强度 vs 性能关联
5 种漂移指标 × 5 个性能指标 = 30 对相关性检验。核心发现：漂移的量和分类性能没有显著线性相关（Pearson r=-0.09, p=0.84），但漂移与 R²（r=-0.88, p=0.008）和 MAE（r=0.87, p=0.011）显著相关。漂移的"方向"比"量"更关键——取决于偏移是否穿过决策边界。产出：`drift_performance_association/`。

### 4.6 浓度回归 DNN
3 种方案：sensor only / +batch ID / +sliding history baseline。单阶段+历史窗口 3 在多数批次最优；Batch 4 对所有方案出现负 R²。产出：`concentration_drift_results/` + 5 张图。

### 4.7 Batch 10 错误逐例分析
4 模型混淆矩阵 + 气体×浓度区间准确率 + 逐样本预测。乙烯↔甲苯是最严重的双向混淆对。产出：`figures/error_*.png` + `错误分析_气体与浓度区间.md`。

### 4.8 消融实验
Fusion DNN 的 4 个变体（full / no_time / no_reg / no_dropout）× 7 batch。结论：时间分支和回归辅助损失都没有提升分类。产出：`drift_results_ablation/` + `FusionDNN消融实验结果.md`。

## 5. 已完成工程优化（2 个）

| 优化 | 方法 | 结论 |
|------|------|------|
| 历史窗口选择 | w=1~5 对比，Batch 4-8 平均 Macro-F1 | **w=3 最优** |
| 低浓度残差校准 | 历史残差偏移修正 | 产出：`lowppm_residual_results/` |

## 6. 已完成专项实验（5 个）

### 6.1 多任务 DNN 损失权重选择
对比 λ=0.15/0.35/0.60。开发集选 λ=0.60。最终确认：分类基本不降（-0.4%），低浓度 MAE 从 26.43 降到 20.23 ppm。产出：`multitask_weight_dev_*/` + `多任务DNN损失权重选择结果.md`。

### 6.2 Fusion 历史校准多任务 DNN
不用 Batch 编号，改用历史基线变化量作为第二分支输入。浓度预测大幅改善——低浓度 MAE 从 26.43 砍到 10.90 ppm（降低 58%），R² 从 0.773 升到 0.839。目前浓度/低浓度预测最强的模型。产出：`fusion_history_dev_*/` + `Fusion历史校准多任务DNN实验结果.md`。

### 6.3 甲苯专项鲁棒分类
对比 CE / Weighted CE（甲苯×2）/ Focal Loss。加权 CE 在开发集有效（Toluene +3%），但 Batch 9 甲苯 Recall 仅从 1.0%→5.9%，Batch 10 无改善。结论：甲苯困难不是权重问题，是在特征空间中和乙烯不可分。产出：`toluene_robust_*/` + `甲苯专项鲁棒分类实验结果.md`。

### 6.4 MMD 领域自适应（★ 正向突破）
在训练 loss 中加入 MMD 正则化项，强制编码器将不同批次数据映射到相似特征分布。λ=0.5 最优，**Batch 10 准确率从 67.6% → 71.4%（+3.8%）**——所有实验中第一个能切实改善远期分类的方法。改善集中在边界附近的 Ethylene（+6.4%）和 Acetone（+13.4%）。经 3 随机种子稳定性验证，Batch 10 改善方向完全一致（+1.56% / +1.56% / +1.61%），确认可复现。MMD 不增加模型参数、不依赖测试 Batch 信息。产出：`run_domain_adaptation.py`、`domain_adaptation_results/` + `领域自适应实验结果.md`、`run_mmd_stability.py` + `mmd_stability_results/` + `MMD多随机种子稳定性验证结果.md`。

### 6.5 Fusion × MMD 叠加（★ 负结果对照）
将 MMD 同时施加到 Fusion 双分支编码器。基线全面优于 MMD，Batch 7 准确率暴跌 9.8%。失败原因：历史分支的职责是报告漂移量，MMD 强制对齐等于抹掉核心信息。与单分支 MMD 正结果构成完整对照——证明 MMD 有效性依赖于编码器架构类型。产出：`run_fusion_mmd.py`、`fusion_mmd_results/` + `FusionMMD领域自适应实验结果.md`。

## 7. 加权 MMD 系列实验（路线穷尽验证）

### 7.1 困难气体加权 MMD 初步筛选
在 MMD λ=0.5 基础上对比四种方案（基线 / 困难气体加权 / 低浓度加权 / 联合加权）。开发集选困难气体加权（Ethylene/Acetone/Toluene 统一 1.5）。Batch 10 Ethylene 从 36.2% → 47.2%（+11.0%），但 Acetone 和 Toluene 下降。产出：`run_mmd_weighted.py`、`mmd_weighted_results/` + `加权MMD领域自适应实验结果.md`。

### 7.2 困难气体加权 MMD 稳定性验证
3 种子 × 7 Batch = 42 次训练。Batch 10 改善在两个种子上正向、一个负向，均值仅 +0.11%，方向不一致。推翻了单种子下"困难气体加权有效"的初步结论。产出：`run_mmd_weighted_stability.py`、`mmd_weighted_stability_results/` + `加权MMD稳定性验证结果.md`。

### 7.3 细粒度加权 MMD
假设三类气体失败机制不同，设 Ethylene=1.5 / Acetone=1.2 / Toluene=1.0。开发集排名第二，仍劣于旧方案，且 Toluene 仍被牺牲。产出：`run_mmd_fine_weighted.py`、`fine_weighted_mmd_results/` + `细粒度加权MMD实验结果.md`。

### 加权路线汇总
三个加权实验全部指向同一结论：在 MMD 已对齐分布的前提下，类别权重无法提供额外稳定增益。加权路线宣告穷尽。当前分类最优锁定为普通 MMD（λ=0.5，无加权）。

## 8. 对照实验（负结果，有学术价值）

| 实验 | 结果 | 发现 |
|------|:--:|------|
| 困难气体加权 | Batch 10 -1.2% | 直接加权无效 |
| IQR 校准 | Batch 10 -2.2% | 特征过载反降 |
| 选择性校准 | 平均 -1.2%，Toluene 崩盘 | 校准需全 128 传感器协同 |
| Fusion × MMD | 开发集全面劣于基线 | MMD 在多分支中有害 |

## 9. 核心研究发现

1. **时序漂移是真实的**：所有模型在 Batch 10 的准确率比早期批次下降 15-30 个百分点。PCA 可视化直观证实了数据分布随时间偏移，乙烯-甲苯在后期空间中几乎不可分。

2. **漂移方向比漂移量更重要**：漂移强度与分类性能的相关系数仅 -0.09（p=0.84），但与回归指标有显著相关（R² r=-0.88, MAE r=0.87）。漂移是否损害分类，取决于偏移方向是否穿过决策边界。

3. **Toluene-Ethylene-Acetone 构成最难分的三角**：PCA、混淆矩阵和加权实验一致证实，这三类气体在漂移后高度重叠，且属于本质上的特征不可分而非模型不够重视。

4. **低浓度天然更难**：≤25 ppm 准确率比 200+ ppm 低 30-40 个百分点。

5. **MMD 领域自适应是唯一突破天花板的方向**：单分支 MMD 在 Batch 10 上稳定改善 +1.5~1.6%，Ethylene +3%，Acetone +7%。但 MMD 仅在单通路架构中有效——加到 Fusion 双分支上反而有害，因为历史分支的漂移报告功能被对齐操作破坏。

6. **加权路线穷尽**：困难气体统一加权、细粒度加权、甲苯专项加权均无法在 MMD 基础上提供额外稳定增益。Toluene 的困难源于训练样本不足而非模型注意力不够。

7. **不同改进方法不能简单叠加**：选择性校准破坏协同信息、Fusion×MMD 损害历史分支功能、加权策略在分布已对齐后无效。模型鲁棒性改进需要方法、架构和特征职责之间的精确匹配。

## 10. 最终模型选择表

| 目标 | 最优模型 | 关键指标 |
|------|------|:---:|
| 总体分类 | 普通 MMD（λ=0.5） | Batch 10 Acc 71.4%, F1 0.719 |
| 远期强漂移 Batch 10 | 普通 MMD（λ=0.5） | 三种子稳定改善 +1.5~1.6% |
| 浓度预测 | Fusion 历史校准多任务 DNN | ≤50ppm MAE 10.90, R² 0.839 |
| 低浓度预测 | Fusion 历史校准多任务 DNN | ≤50ppm MAE 10.90 |
| 硬件部署 | 普通 MMD（λ=0.5） | 不依赖 Batch ID，只输传感器数据 |

## 11. 报告核心主线

> 首先利用滚动时序验证证明气体传感器数据存在显著批次漂移，普通识别模型在未来 Batch 中性能下降；随后通过 PCA、混淆矩阵、边界样本和低浓度分析定位失效来源——漂移方向是否穿过决策边界比漂移量更关键，Toluene-Ethylene-Acetone 构成最难分的三角；再比较多种校准、融合和领域自适应方法；最终发现单分支 MMD 在强漂移远期 Batch 中能稳定改善乙烯、丙酮等边界气体的识别，而 Fusion 架构中直接施加 MMD 会损害漂移信息表达，选择性校准破坏协同信息，加权策略在分布对齐后无法提供额外增益。因此，模型鲁棒性改进需要与传感器时序特征的作用相匹配，不是"加更多模块"就能解决问题。

## 12. 实验进度对照申请书

| 申请书计划 | 对应实验 | 状态 |
|------------|----------|:--:|
| 4-5月：选题确定 | `source.doc` 定稿 | ✅ |
| 6-7月：数据采集清洗 | `all_batches.csv` 构建、128维特征提取 | ✅ |
| 8-9月：模型搭建训练 | 13 种模型全部跑完 | ✅ |
| 8-9月：第一轮性能评估 | 8 个分析维度 + 2 个工程优化 + 5 个专项实验 + 4 个对照实验 | ✅ |
| 8-9月：中期报告 | 实验全部完成，可开始撰写 | 🟡 |
| 10-11月：深度测试 | 加权路线穷尽、MMD 锁定最优方案 | 🟡 |
| 12-1月：优化与论文 | — | ⬜ |
| 2-4月：答辩 | — | ⬜ |

## 13. 后续方向

**报告撰写**：当前所有计划内实验已全部完成，数据和结论完整，可直接开始写报告。核心叙事线清晰——"数据→测试→发现→改进→对照→结论"的完整研究链。

**对抗域自适应（Gradient Reversal Layer）**：在单分支编码器后接域分类器，通过梯度反转迫使编码器抹除批次信息，与 MMD 形成方法对比。论文价值高，但需要 GPU。

**数据增强**：基于已有 128×10 条漂移轨迹做插值，生成虚拟漂移批次扩充训练集，不增加新数据。

**硬件迁移**：待组员提供 `hardware_data.csv`，将 MMD 方法和滑动历史基线在真实传感器数据上验证。

## 14. 给组员的协作信息

- **所有代码和结果已推送**: `git@github.com:zxm628810-alt/srtp.git` (main 分支)
- **核心脚本**: `run_drift_experiments.py` (7 种 sklearn 模型)、`run_domain_adaptation.py` (MMD 领域自适应)、`run_ablation_study.py` (消融)、`run_selective_calibration.py` (选择性校准)、`run_fusion_mmd.py` (Fusion×MMD)、`run_mmd_weighted.py` (加权 MMD)、`run_mmd_fine_weighted.py` (细粒度加权)
- **稳定性验证**: `run_mmd_stability.py`、`run_mmd_weighted_stability.py`
- **优先级**: 启动报告撰写；等待硬件数据
