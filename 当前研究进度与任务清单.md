# SRTP 当前研究进度与任务清单

更新时间：2026-08-02。项目核心不是直接做商品化检测仪，而是研究**气体传感器时序漂移对识别模型的影响、测试方法和改进方法**。

## 1. 已完成

- 已将 UCI Gas Sensor Array Drift 数据集的 Batch 1--10 解析为统一数据表 `all_batches.csv`：13,910 条样本，128 个传感器特征、气体类别、浓度和批次号。
- 已核对原始 `.dat` 的真实类别编码：1=Acetone、2=Acetaldehyde、3=Ethanol、4=Ethylene、5=Ammonia、6=Toluene；训练脚本已按此修正。
- 已完成 RandomForest 基线模型、普通 DNN（sklearn MLP）以及“DNN+批次号”的固定早期训练和滚动时序验证。
- 已完成“DNN+训练历史基线相对特征”的滚动时序验证。基线取训练集中最早批次的各传感器中位数，测试批次的标签和统计量均未参与该变换，因此不存在测试信息泄漏。
- 已为每一轮实验输出准确率、Macro-F1、每种气体召回率、混淆矩阵，以及独立的浓度回归基线结果。

## 2. 最新滚动验证结论

完整指标见 `滚动验证模型对比.csv`。

- 测试规则：用 Batch 1 到 k-1 训练，用从未见过的 Batch k 测试；k=4--10。
- 普通 DNN 在 Batch 10 的准确率为 **69.5%**，比 RandomForest 的 **61.0%** 高，但距离稳定鲁棒仍有差距。
- 加入原始批次号后，Batch 10 提升到 **71.2%**，但 Batch 4、7--9 没有稳定胜出。因此不能写成“加入时间特征一定提升鲁棒性”；正确结论是：**简单的批次号融合存在有限、非稳定的帮助，仍需改进融合方式。**
- 基线相对特征在 Batch 7 和 Batch 8 分别达到 **81.7%**、**93.9%**，但 Batch 10 仍为 **68.1%**。这表明简单基线校准可以改善部分批次，却不足以抵抗长期漂移。
- Toluene 在若干批次的召回率较低，后续应结合各批次样本数和混淆矩阵单独分析，不能只看总体准确率。

## 3. 当前最优先任务（按顺序）

1. **结果可视化**：画四模型的“准确率 / Macro-F1 随测试批次变化曲线”；再生成逐气体召回率热力图和关键批次混淆矩阵图。
2. **漂移分布分析**：用 PCA 将 Batch 1--10 的 128 维特征降至二维，观察同一气体随时间移动的方向与类别重叠区。这是申报书中的决策边界覆盖测试证据。
3. **改进模型设计**：在不使用测试集信息的前提下，比较更合理的漂移补偿：训练历史基线、滑动历史统计量、领域归一化，和 DNN 融合模型。每种方法必须和 RF / 普通 DNN 用同一滚动测试比较。
4. **浓度任务单独评估**：目前输出的是 RandomForest 浓度回归基线，后续应实现 DNN 浓度回归或“分类+回归”双任务模型，并报告每个未来批次的 MAE、R²。
5. **形成报告材料**：将方法、数据划分原则、指标表、曲线、典型错误案例写入中期报告；结论必须强调跨批次测试，不使用随机划分的接近 100% 准确率作为核心证据。

## 4. 与组员协作 / GitHub 状态

- 本地缓存的 `origin/main` 位于提交 `1140041`，包含时序漂移实验和 DNN 评估。
- 本次尝试拉取 GitHub 时网络无法连接 GitHub 443 端口，故无法确认远端是否刚出现新提交。
- 若组员已完成代码、图表或报告，请其在自己的电脑执行 `git add`、`git commit`、`git push` 到 `main`，或提交 Pull Request；推送成功后即可被同步和纳入下一轮分析。

## 5. 报告可用核心结论（保留）

实验表明，传感器阵列数据存在显著的跨批次分布变化，导致传统随机划分下的高准确率无法反映未来批次识别性能。引入滑动历史基线校准后，模型在部分后期批次有所改善，Batch 10 准确率由普通 DNN 的 69.5% 提升至 70.1%；但增益不稳定，表明时序漂移具有复杂的类别相关特征。后续将围绕困难气体类别和低浓度样本，研究自适应融合与加权训练方法。

该项目的核心并非单纯寻找最高准确率模型，而是建立“数据处理—跨时序测试—错误定位—抗漂移改进—公平验证”的研究流程，定量说明传感器时序漂移如何导致模型退化，以及改进方法能缓解到何种程度。

## 6. 第二项改进实验：困难气体与低浓度加权

对乙烯、丙酮、甲苯训练样本设置 2 倍权重，并对浓度不高于 50 ppm 的训练样本额外设置 1.5 倍权重。完整的 Batch 4--10 滚动验证显示：该初始权重方案在部分批次（如 Batch 4、7）提高了指标，但在关键 Batch 10 中总体准确率从普通 DNN 的 69.5% 降至 68.3%，低浓度准确率也由 65.3% 降至 63.6%。甲苯召回率略升，但乙烯和丙酮没有改善。

结论是：直接困难样本加权不足以稳定解决时序漂移下的类别边界问题。后续应将加权方法与滑动历史校准、特征融合或更细粒度的权重调参结合，而不能宣称该初始方案已提升鲁棒性。

## 7. 第三项改进实验：历史中位数 + IQR 校准

在滑动历史中位数之外，新增了逐传感器历史 IQR 以及 IQR 标准化偏移特征，形成 384 维 DNN 输入。该方法在 Batch 6 的 Macro-F1 达到 71.8%，说明某些批次存在收益；但 Batch 10 准确率为 67.3%，低于普通 DNN 和此前的滑动历史基线。结论是：仅增加历史波动范围特征不能稳定改善长期漂移，后续需要使用历史批次进行窗口大小选择，或设计与困难气体边界直接相关的融合方法。
