# SRTP 项目执行清单（基于当前硬件）

## 一、项目最终要做什么

构建一套基于 ESP32-S3 的便携式气体/环境监测装置。装置实时采集 SGP30 的 TVOC、eCO2，JED117-001 的丙酮浓度，以及 DHT22 的温湿度；在 ST7735 TFT 屏幕显示结果，并将数据通过串口或 Wi-Fi 保存。基于真实硬件数据建立模型，用于丙酮状态判断、数据异常识别和环境条件下的读数校准。

**当前可交付的核心功能**：

1. 实时显示 TVOC、eCO2、丙酮 ppm、温度、湿度。
2. 按阈值提示“正常 / 注意 / 警告”。
3. 保存传感器数据，形成可复现的数据集。
4. 用真实硬件数据训练模型，判断 `clean / acetone / warning` 等状态。
5. 对丙酮读数做温湿度与空气质量信号关联分析，识别异常读数。

## 二、现有硬件与职责

| 模块 | 已知接口/引脚 | 采集结果 | 在项目中的作用 |
|---|---|---|---|
| ESP32-S3 N16R8 | Arduino，3.3V，串口115200 | — | 主控、采集、显示、通信 |
| SGP30 | I2C；SDA GPIO21，SCL GPIO14 | TVOC、eCO2 | 综合 VOC/空气质量特征 |
| JED117-001 MEMS 丙酮传感器 | I2C；地址0x54；0–500 ppm | 丙酮 ppm | 丙酮主测量值 |
| DHT22 | DATA GPIO17 | 温度、湿度 | 环境补偿特征 |
| ST7735 TFT | SCK18、MOSI13、CS5、DC2、RST4 | — | 本地显示 |
| MiCS-4514 | 尚未接线 | CO/NO2/NH3（待验证） | 暂不进入最终模型 |

所有模块使用 3.3V 并共地。

## 三、你负责的模型工作

### 1. 明确最终模型输入与输出

输入字段固定为：

```text
timestamp, TVOC, eCO2, mems_acetone_ppm, temperature, humidity
```

输出设计为：

```text
acetone_state: clean / acetone / warning
acetone_ppm: MEMS读数
data_quality: normal / suspicious
confidence: 模型置信度
```

**做法**：先把 ESP32 每 1–5 秒输出一行 CSV；保存时补充人工标签 `label`，例如 `clean`、`acetone`、`warning`。

### 2. 建立原始数据采集集

文件名：`hardware_data.csv`

```csv
timestamp,TVOC,eCO2,mems_acetone_ppm,temperature,humidity,label
2026-08-02T10:00:00,95,415,0.2,25.1,51.0,clean
2026-08-02T10:05:00,210,560,18.3,25.2,51.4,acetone
```

**最低采集计划**：

- 清洁空气：至少 100 条，分不同时间、温湿度记录。
- 低/中/高丙酮条件：每档至少 100 条，必须记录可靠的标准浓度或明确实验条件。
- 每次开机：记录预热阶段和稳定阶段，训练时仅使用稳定阶段数据。
- 异常场景：遮挡、断线、超量程、传感器刚启动等要单独标注。

> 不应自行在普通环境中配制或大量挥发丙酮；应在符合安全规范的场地，用老师批准的标准气体/标准样品与通风措施完成采集。

### 3. 训练硬件专用模型

程序：`train_hardware_model.py`

```powershell
cd C:\srtp
python train_hardware_model.py --csv hardware_data.csv
```

模型做两件事：

- 用 `TVOC、eCO2、MEMS丙酮、温度、湿度` 识别气体状态。
- 用 `TVOC、eCO2、温度、湿度` 估计与 MEMS 丙酮读数是否一致；明显偏离时标记 `suspicious`，用于发现异常数据或漂移。

评价指标：分类准确率、各类别召回率、丙酮一致性 MAE、R2。

### 4. 校准与阈值策略

JED117-001 已经输出 ppm，模型不是替代传感器读数，而是提升稳定性和可解释性。

**做法**：

1. 在清洁空气中预热后记录基线。
2. 按说明书完成校零并复读确认。
3. 比较不同温湿度下同一条件的 ppm 偏差。
4. 根据实测和老师给出的安全要求，确定正常/注意/警告阈值。
5. 不将未经标准气体校准的读数用于安全承诺或医疗诊断。

## 四、硬件采集工作

### 1. 先完成单模块读取

| 顺序 | 要完成的事项 | 通过标准 |
|---|---|---|
| 1 | DHT22 读取温湿度 | 串口稳定输出温度、湿度 |
| 2 | SGP30 读取 TVOC、eCO2 | I2C 不报错，读数随环境变化 |
| 3 | JED117-001 读取丙酮 | 预热后读出 0–500 ppm 数值 |
| 4 | ST7735 显示 | 屏幕显示所有字段 |
| 5 | 合并采集 | 每 1–5 秒输出一行完整 CSV |

### 2. JED117-001 特别要求

- I2C 地址：`0x54`。
- 浓度计算：`ppm = high_byte * 256 + low_byte`。
- 短期上电预热约 30 秒；长期断电存放后需按说明书延长预热。
- 使用 I2C 上拉电阻；SDA/SCL 与 SGP30 共用总线前，先确认模块没有地址冲突。
- 手册文字中的读取命令为 `0xA1`，附录代码为 `0xE1`，存在冲突。必须用实际模块试读，确认可用命令后记录在代码和报告中。

## 五、软件联调流程

```text
传感器 → ESP32读取 → 串口CSV/Wi-Fi → 电脑保存 → 数据清洗 → 模型训练
    ↑                                                       ↓
    └──────── TFT显示 ← 模型状态/阈值判断 ← 校准参数 ──────┘
```

联调顺序：

1. ESP32 只输出原始传感器数据。
2. 电脑端保存 `hardware_data.csv`，检查字段、单位、缺失值和时间顺序。
3. 用训练脚本得到 `hardware_gas_model.joblib`。
4. 用留出的测试数据验证，不能只看训练集结果。
5. 把简单阈值/轻量模型规则迁移到 ESP32；复杂模型可先在电脑或树莓派运行。

## 六、现有模型如何处理

| 模型 | 当前用途 | 能否直接部署到当前硬件 |
|---|---|---|
| `gas_classifier.joblib` | MQ 数据集分类演示 | 不能，输入是7个 MQ传感器 |
| `uci_gas_model.joblib` | UCI专业传感器多气体研究 | 不能，输入是128个专业传感器特征 |
| `ammonia_model.joblib` | UCI氨气研究 | 不能，当前没有NH3硬件输入 |
| `acetone_model.joblib` | UCI丙酮研究 | 不能直接部署，可作算法对照 |
| `hardware_gas_model.joblib` | 当前硬件的最终目标模型 | 等真实 `hardware_data.csv` 后训练 |

## 七、团队分工建议

| 角色 | 任务 |
|---|---|
| 硬件同伴 | 接线、ESP32 驱动、TFT 显示、串口/Wi-Fi 输出 |
| 你（模型） | 数据格式、清洗、标签、训练、评价、校准、异常检测 |
| 共同完成 | 实验设计、采样、安全规范、阈值设定、整机联调、答辩材料 |

## 八、阶段成果与验收

1. **阶段一：硬件通电**：所有模块稳定读数并显示。
2. **阶段二：数据集**：形成可追溯的 `hardware_data.csv`，含标签和实验记录。
3. **阶段三：模型**：输出分类准确率、混淆矩阵、MAE、R2，保存模型文件。
4. **阶段四：联调**：实时输入能得到状态、丙酮 ppm 和异常提示。
5. **阶段五：报告**：写明硬件架构、数据来源、算法、实验设置、结果、局限与改进。

## 九、当前立刻要做的三件事

1. 硬件组先确认 JED117-001 的 SDA/SCL 实际接到了 ESP32-S3 哪两个 GPIO，并成功读出 ppm。
2. 让 ESP32 串口输出完整 CSV 行；先采集至少 20 条稳定空气数据检查格式。
3. 将采集的 CSV 发给模型负责人，用 `train_hardware_model.py` 做第一版真实硬件训练。
