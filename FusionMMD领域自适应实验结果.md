# Fusion × MMD 领域自适应实验结果

## 目的

此前两个独立实验分别取得了正向结果：

- **MMD 领域自适应**（普通 3 层 MLP）：Batch 10 分类准确率 +3.8%，改善集中在边界附近的 Ethylene 和 Acetone
- **Fusion 历史校准多任务 DNN**（双分支架构）：低浓度 MAE 砍到 10.90 ppm，浓度预测目前最强

本实验将两者叠加——在 Fusion 双分支编码器上各自施加 MMD 分布对齐，期望在分类和浓度上同时获得改善。

## 方法

模型结构与 Fusion 历史校准多任务 DNN 完全一致：传感器分支（128→256→128）+ 历史校准分支（128→128→64）→ 拼接融合 → 分类头 + 回归头。唯一区别：训练时对传感器分支的 128 维输出 `z_s` 和历史分支的 64 维输出 `z_h` **分别施加 MMD 正则化**，各自将 old-batch 和 new-batch 的表示分布对齐。

总损失 = CrossEntropy + 0.60 × SmoothL1（回归，固定） + λ_mmd × (MMD_sensor + MMD_history)。

## 权重选择

仅在 Batch 4-8 上对比 λ_mmd=0（基线）、0.1、0.5，以四项指标等权排名（分类准确率 ↑、Macro-F1 ↑、MAE ↓、≤50ppm MAE ↓）。λ_reg=0.60 固定（来自多任务权重选择实验的最优值）。

### 开发阶段结果（Batch 4-8 平均）

| λ_mmd | 准确率 | Macro-F1 | MAE ppm | ≤50ppm MAE | 综合排名 |
|:---:|:---:|:---:|:---:|:---:|:---:|
| **0（基线）** | 84.3% | **0.771** | **26.56** | **28.70** | **1.25** |
| 0.1 | **84.5%** | 0.763 | 27.02 | 28.66 | 1.75 |
| 0.5 | 83.7% | 0.749 | 31.25 | 34.60 | 3.00 |

**λ_mmd=0（基线）在四项指标中三项第一，MMD 被淘汰。**最终 Batch 9-10 仅报告基线结果。

## 最终确认（Batch 9-10，仅基线）

| Batch | 准确率 | Macro-F1 | MAE ppm | R² | ≤50ppm MAE |
|:---:|:---:|:---:|:---:|:---:|:---:|
| 9 | 76.6% | 0.753 | 15.81 | 0.798 | 8.64 |
| 10 | 67.6% | 0.678 | 54.14 | 0.880 | 13.15 |

浓度预测表现与之前的 Fusion 历史校准多任务 DNN 一致，分类低于普通 MMD MLP 的 71.4%。

## MMD 在 Fusion 上失效的集中表现：Batch 7

| λ_mmd | Batch 7 准确率 | Toluene Recall | Ethylene Recall |
|:---:|:---:|:---:|:---:|
| 0（基线） | 85.9% | 0.620 | 0.647 |
| 0.1 | 80.6% | 0.431 | 0.540 |
| 0.5 | **76.1%** | **0.220** | 0.462 |

MMD 随 λ 增大在 Batch 7 上单调恶化：准确率 -9.8%，Toluene Recall 从 0.620 崩到 0.220。这与逐通道选择性校准在 Batch 7 上的失败模式高度一致——都是在 Toluene 这一最困难气体上崩溃。

## 结论

1. **MMD 领域自适应在 Fusion 双分支架构上失效，甚至有害。**基线（不加 MMD）在四项指标中三项最优，λ=0.5 时 Batch 7 准确率暴跌 9.8%。

2. **失败机制分析**：MMD 在单分支 MLP 上有效（+3.8%），因为"消除分布偏移"就是编码器的唯一目标。但在 Fusion 架构中，历史校准分支的职责恰恰是**向融合层报告漂移量**——不同 Batch 的漂移偏移量天然应该不同（老 Batch 偏移小、新 Batch 偏移大）。MMD 强制将不同 Batch 的历史分支表示对齐，等于抹掉了"这个样本偏了多少"的关键信息。融合层等不到漂移量信息，两个分支的互补性被破坏，分类性能反而下降。

3. **对后续研究的指导**：如果要在多分支架构中引入领域自适应，不应同时对两个分支做 MMD——应仅对齐传感器分支（其漂移是应消除的物理噪声），而保留历史分支的批次差异性（其报告漂移量恰恰是有价值的信息）。或者改用对抗域自适应替代 MMD，让域分类器的梯度仅回传到传感器分支。

4. **正向价值**：该负结果与单分支 MMD 的正结果形成对照，共同证明了"MMD 的有效性依赖于编码器架构——单通路结构中有效，多分支结构中有害"。两个实验放在一起，比单独一个正结果有更强的学术论证力。

## 可复核文件

- 结果汇总：`fusion_mmd_results/fusion_mmd_results.csv`
- 对比图：`fusion_mmd_results/figures/FusionMMD开发阶段对比.png`、`FusionMMD最终确认.png`、`FusionMMD_Batch7崩溃分析.png`
- 脚本：`run_fusion_mmd.py`
