# Fusion DNN 消融实验结果

## 目的

消融实验在相同滚动规则下测试 Fusion DNN 的三个设计是否真正贡献跨时序气体识别性能：

- `full`：传感器分支 + 时间分支 + Dropout + 分类/回归联合损失；
- `no_time`：去掉时间分支；
- `no_reg`：去掉浓度回归辅助损失，只训练分类；
- `no_dropout`：关闭全部 Dropout。

所有模型均采用 Batch 1～k-1 训练、Batch k 测试，覆盖 Batch 4～10。由于仓库未同步原始 `.dat`，脚本已兼容项目统一的 `all_batches.csv`；其 128 维特征、气体类别、浓度、批次与原始解析结果一致。

## 平均分类结果

| 变体 | Batch 4～10 平均准确率 | 平均 Macro-F1 | Batch 9～10 平均准确率 | Batch 9～10 平均 Macro-F1 |
|---|---:|---:|---:|---:|
| full | 0.8330 | 0.7737 | 0.7534 | 0.7473 |
| no_time | 0.8353 | 0.7752 | 0.7600 | 0.7538 |
| no_reg | **0.8400** | **0.7818** | 0.7653 | 0.7601 |
| no_dropout | 0.8265 | 0.7709 | **0.7732** | **0.7651** |

Batch 10 中：`full` 准确率 73.4%、Macro-F1 73.7%；`no_reg` 为 77.5%、77.6%；`no_dropout` 为 77.2%、77.3%。

## 结论

1. **时间分支未带来稳定收益。**`no_time` 的平均分类结果略高于 `full`，表明直接使用 Batch 时间特征并不能稳定提升未见未来批次的分类泛化。这与此前“直接批次号帮助有限”的结论一致。
2. **浓度回归辅助损失未提高分类。**去掉回归损失的 `no_reg` 在 Batch 4～10 的平均准确率和 Macro-F1 都最高。因此不能把“联合回归损失提高分类性能”写为结论。
3. **Dropout 的作用随时段变化。**全时段平均上，保留 Dropout 更好；但 Batch 9～10 中 `no_dropout` 反而最好，说明正则化强度也受漂移阶段影响。
4. **完整 Fusion DNN 仍有研究价值，但不是当前分类最优。**它提供了一个可用于研究多模态融合的对照结构；当前证据更支持“历史校准特征”而非直接 Batch 时间特征。
5. 本次消融输出的是分类指标，尚未对每个变体输出浓度 MAE/R²。因此不能据此否定回归辅助任务对浓度预测的价值；我们已有的 Fusion 历史校准多任务实验表明，联合训练可以改善浓度与低浓度预测。

## 可复核文件

- 汇总：`drift_results_ablation/ablation_summary.csv`
- 各 Batch 混淆矩阵：`drift_results_ablation/confusion_ablation_*.csv`
- 脚本：`run_ablation_study.py`
