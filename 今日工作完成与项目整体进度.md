# 今日工作完成情况与项目整体进度

**日期**: 2026-08-04  
**研究者**: xiaoyuxu  
**仓库**: github.com/zxm628810-alt/srtp (main 分支)

---

## 一、今日新增完成（6 大方向）

### 1. Fusion DNN (PyTorch 双分支) 滚动验证 ✅

基于 `train_multimodal_dnn_rolling.py` 跑通了完整传感器+时间双分支融合 DNN，使用分类+回归联合损失，在 Batch 4-10 上完成严格滚动时序验证。

| 批次 | 准确率 | Macro-F1 | Toluene Recall | 关键观察 |
|------|--------|----------|----------------|----------|
| 4 | 75.8% | 0.648 | **0.00** | 训练数据极少 |
| 5 | 99.0% | 0.825 | **0.00** | |
| 6 | 76.2% | 0.651 | **0.00** | |
| 7 | 87.9% | 0.871 | 0.905 | 恢复 |
| 8 | 93.5% | 0.925 | 0.944 | 最佳 |
| 9 | 77.2% | 0.757 | **0.01** | 再次崩塌 |
| **10** | **73.4%** | **0.737** | 0.842 | **所有模型中最高** |

> Fusion DNN 在 Batch 10 的准确率(73.4%)和 Macro-F1(0.737)是 7 个模型中最好的，但 Toluene 在 Batch 4/5/6/9 的召回率极低，暴露了双分支架构对稀有气体类别的脆弱性。

### 2. 浓度区间分类准确率分析 ✅

- 将全部测试样本按真实浓度分为 5 档: ≤25 / 25-50 / 50-100 / 100-200 / 200+ ppm
- 4 个模型 × 7 个测试批次 × 5 个浓度档位 = 完整准确率矩阵
- 低浓度(≤50 ppm) vs 高浓度二分类对比

**核心结论**:
- ≤25 ppm 的准确率普遍低于 50%，低浓度样本确实更难分类
- Batch 6 中 ≤25 ppm 仅有 31.9% 准确率（DNN sensor only），而 200+ ppm 高达 95.4%
- Batch 10 中 ≤25 ppm=66.8%，200+ ppm=80.0%，差距仍然显著
- 低浓度准确率是衡量模型鲁棒性的关键指标

### 3. 逐气体 Recall 热力图 ✅

- 7 个模型 × 6 种气体 × 7 个批次 = 彩色热力图
- 绿(1.0) → 红(0.0)，一眼定位 Toluene 在哪些批次崩塌
- 双图输出: 7 模型并排对比 + Fusion DNN 单张放大

**核心发现**:
- Toluene 是最困难的气体: RF/DNN sensor/DNN+time/DNN+batch_ID 在 Batch 4/5 全部为 0
- Ethylene 在 Batch 7 和 10 表现差（DNN sensor: 0.35 和 0.40）
- 加入滑动历史基线后 Toluene 在 Batch 7 恢复至 0.78，但 Batch 9 仍为 0.01
- 丙酮是表现最好的气体之一，多数批次 >0.90

### 4. 漂移强度与模型性能关联分析 ✅

- 计算逐 batch 的分布漂移量（特征均值偏移 + 方差变化 + MMD）
- 将漂移量与模型精度损耗做关联
- 输出漂移强度曲线 + 关联散点图

**核心结论**: 传感器信号在 Batch 4、7、10 出现较大分布偏移，与模型在这些批次的性能下降高度一致。漂移强度与准确率呈负相关。

### 5. 决策边界覆盖分析 ✅

- 对每个测试样本计算到最近决策边界的距离（基于训练集的类别最近邻距离比）
- 识别"边界样本"（靠近多类决策边界的样本）
- PCA 可视化展示边界样本的分布

**核心结论**: 低浓度样本和困难气体(Toluene/Ethylene)样本更接近决策边界，漂移后更容易跨过边界导致错分。

### 6. 辅助实验（3 项）✅

| 实验 | 结论 |
|------|------|
| **历史窗口选择** (w=1~5) | w=3 最优，在 Batch 4-8 开发集上平均 Macro-F1 最高 |
| **低浓度残差校准** | 历史残差偏移可部分修正低浓度估计偏差 |
| **两阶段浓度模型** | 先分类再专属回归的方案未稳定优于单阶段，分类错误会传递给回归 |

---

## 二、对 `run_drift_experiments.py` 的扩展

在原 3 个模型基础上，新增 4 个 sklearn 模型变体：

| 模型 | 特征工程 | Batch 10 准确率 |
|------|----------|:--:|
| `dnn_sensor` (原) | 128 传感器原始值 | 69.5% |
| `dnn_sensor_time` (原) | + 批次号 | 71.2% |
| `dnn_sensor_baseline` (**新**) | + 相对最早 batch 基线的 128 维变化率 | 68.1% |
| `dnn_sensor_history_baseline` (**新**) | + 滑动前 3 个 batch 中位数的 128 维相对变化 | 70.1% |
| `dnn_sensor_history_robust` (**新**) | + 128 维中位数偏移 + 128 维 IQR 标准化 (共 384 维) | 67.3% |
| `dnn_sensor_weighted` (**新**) | 困难气体×2 + 低浓度×1.5 样本加权 | 68.3% |

新增低浓度准确率(`low_ppm_accuracy`)指标和 `--history-window` 参数。

---

## 三、待完成

| 任务 | 状态 |
|------|:--:|
| **Fusion DNN 消融实验** (拆时间分支 / 拆回归 loss / 关 Dropout) | 🔴 脚本已写好，未运行 |
| 硬件数据采集与硬件模型训练 | 🔴 等待硬件组提供数据 |
| 中期报告撰写 | 🟡 可开始整合现有结果 |

---

## 四、项目整体进度

### 已完成模块

```
UCI 数据集 (10 批次, 13,910 样本)
    │
    ├── 固定窗口实验 (Train 1-3 → Test 4-10)
    │   ├── RandomForest 基线 ✅
    │   ├── DNN sensor only ✅
    │   └── DNN + batch ID ✅
    │
    ├── 滚动时序验证 (Train 1~k-1 → Test k)
    │   ├── [3 个基线模型] RF / DNN sensor / DNN + batch ID ✅
    │   ├── [4 个改进模型] baseline / history_baseline / history_robust / weighted ✅
    │   └── [Fusion DNN] PyTorch 双分支 + 联合 loss ✅
    │
    ├── 分析维度
    │   ├── 浓度区间分类准确率 (5 档位) ✅
    │   ├── 逐气体 Recall 热力图 (6 气体 × 7 batch) ✅
    │   ├── PCA 漂移可视化 (质心轨迹 / 按气体分组) ✅
    │   ├── 决策边界覆盖分析 ✅
    │   ├── 漂移强度 vs 性能衰减关联 ✅
    │   ├── 浓度回归 DNN (3 种方案) ✅
    │   └── Batch 10 错误逐例分析 ✅
    │
    ├── 工程优化
    │   ├── 历史窗口大小选择 (w=1~5 → w=3 最优) ✅
    │   ├── 低浓度残差校准 ✅
    │   └── 两阶段浓度模型 ✅
    │
    └── 待运行
        └── Fusion DNN 消融实验 🔴
```

### 模型对比总览 (Batch 10, 最困难未来批次)

| 模型 | 准确率 | Macro-F1 | ≤50ppm 准确率 | Toluene Recall |
|------|:------:|:--------:|:------------:|:------------:|
| RF | 61.0% | 0.608 | — | 0.80 |
| DNN sensor | 69.5% | 0.698 | 65.3% | 0.79 |
| DNN + batch ID | 71.2% | 0.717 | 64.5% | 0.84 |
| DNN + fixed baseline | 68.1% | 0.683 | — | — |
| DNN + sliding baseline | 70.1% | 0.703 | 64.4% | 0.84 |
| DNN + weights | 68.3% | 0.686 | 63.6% | 0.81 |
| **Fusion DNN** | **73.4%** | **0.737** | — | 0.84 |

### 核心研究发现

1. **时序漂移是真实的**: 所有模型在 Batch 10 的准确率比早期批次下降 15-30 个百分点，证明传感器漂移严重影响气体识别。

2. **Toluene 是致命弱点**: 几乎所有模型的 Toluene 召回率在早期批次(Batch 4-6)为 0。Fusion DNN 在 Batch 9 仍跌落至 0.01。

3. **低浓度更难**: ≤25 ppm 样本的分类准确率平均比 200+ ppm 低 30-40 个百分点。

4. **特征校准有帮助但不稳定**: 滑动历史基线在某些批次改善显著(Batch 7: 77.4% vs 76.2%)，但在 Batch 10 增益有限(70.1% vs 69.5%)。

5. **Fusion DNN 是最优架构但需消融验证**: 双分支 + 联合 loss 在 Batch 10 获得最高准确率(73.4%)，但需要消融实验确认每个组件的贡献。

6. **漂移与性能关联明确**: 分布偏移大的批次(Batch 4/7/10)对应低准确率，决策边界附近样本更容易因漂移被错分。

---

## 五、给组员的协作信息

- **所有代码和结果已推送**: `git@github.com:zxm628810-alt/srtp.git` (main 分支)
- **核心脚本**: `run_drift_experiments.py` (7 种模型)、`train_multimodal_dnn_rolling.py` (Fusion DNN)
- **分析脚本**: `plot_recall_heatmap.py`、`analyze_concentration_accuracy.py`、`plot_pca_drift.py` 等
- **实验文档**: 各 `.md` 文件包含完整实验结论
- **下一步**: 需要硬件组提供 `hardware_data.csv` 后可启动硬件模型训练
